apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: query-engine-cert
  namespace: query-engine
spec:
  secretName: query-engine-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.domain }}
